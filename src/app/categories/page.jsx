const CategoriesPage = () => {
  console.log(name);
  return (
    <div>
      this is Categories Page where we will see how to catch all segment or
      access multiple dynamic params
    </div>
  );
};

export default CategoriesPage;
